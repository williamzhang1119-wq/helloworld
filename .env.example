# Prefer Anthropic (matches venture1.up.railway.app). OpenAI works as a fallback.
# Model ids are chosen on the server from these variables — the client does not pick a model.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
